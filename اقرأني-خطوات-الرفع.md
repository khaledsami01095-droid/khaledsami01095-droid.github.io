# موقع خالد سامي — خطوات الرفع على جوجل

الفولدر ده هو الموقع كامل: `index.html` + فولدر `media` + ملفات جوجل.
ماتغيّرش أسماء الملفات ولا مكانها.

## 1) الاستضافة (مجاني، 5 دقايق) — Netlify
1. افتح https://app.netlify.com/drop
2. اعمل حساب مجاني (بجيميل).
3. اسحب فولدر `khaled-portfolio` كله وارميه على الصفحة.
4. هيطلعلك لينك زي `random-name-123.netlify.app`. افتحه واتأكد إن الموقع شغال.
5. من Site settings → Change site name → اكتب `khaledsami` عشان اللينك يبقى `khaledsami.netlify.app`.

## 2) الدومين (حوالي 10-15 دولار في السنة)
1. اشتري `khaledsami.com` من Namecheap أو GoDaddy (لو محجوز جرّب `khaledsami.video` أو `khaledsami.me`).
2. في Netlify: Domain management → Add a domain → اكتب الدومين.
3. Netlify هيديك عنوانين DNS (nameservers). حطهم في إعدادات الدومين عند Namecheap.
4. استنى من ساعة لـ 24 ساعة. Netlify بيعمل شهادة HTTPS لوحده.

## 3) الظهور على جوجل
1. افتح https://search.google.com/search-console
2. Add property → اكتب `khaledsami.com` → اختار التحقق بـ DNS (هيديك سطر TXT تحطه عند Namecheap).
3. بعد التحقق: Sitemaps → اكتب `sitemap.xml` → Submit.
4. URL inspection → اكتب لينك الموقع → Request indexing.
5. جوجل بياخد من يومين لأسبوعين. ابحث عن "خالد سامي مونتير" بعدها.

## لو اشتريت دومين غير khaledsami.com
افتح `index.html` وبدّل كل `https://khaledsami.com/` بالدومين الجديد،
ونفس الحاجة في `robots.txt` و `sitemap.xml`. أو ابعتلي الاسم وأنا أعملها.

## لو حبيت تحدّث الموقع بعدين
ارمي الفولدر الجديد على Netlify → Deploys → Drag and drop. اللينك مايتغيرش.
